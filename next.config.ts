import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  images: {
    domains: ["instagram.ffln13-1.fna.fbcdn.net"], // Adiciona o domínio do Instagram
  },
};

export default nextConfig;
